# MouseHaven 项目规则

## 开始前

阅读 `docs/PRODUCT.md`、`docs/P0_TASK.md`、`docs/ACCEPTANCE.md`。当前实施范围只有 P0。不要因读到后续路线而一次做完整产品。

检查当前操作系统、已有 SDK 和仓库文件。不得覆盖已有用户代码；变更保持可审查。不要擅自安装系统组件、提升权限、关闭安全隔离或改变系统配置。

## 不可改写的产品定义

1. 同一个家园、同一只自主生活的鼠鼠。
2. 只有两套视觉表现：横版 2D 与最大化时的 2.5D。不是三种独立游戏场景。
3. 横版 2D 有两个显示尺寸：默认发送按钮量级的微型观察口；点击家园后展开为不太大的 16:9 画面。
4. 默认只显示鼠鼠及周围少量环境，不把整座家园缩进去。跟随的是家园内的镜头，家园窗口锚点不因鼠鼠活动而漂移。
5. 鼠鼠外出不改变家园显示档位；默认微型家园仍保持原大小。点击家园才展开。
6. 点击桌面上外出的鼠鼠触发逃回家；点击家园是展开，不是吓跑她。
7. 任意时刻只有一个角色所有权与一份生活状态。切换窗口不复制角色，不重置动作。
8. 啃咬是纯视觉演出。禁止移动、删除、改名、写入或修改用户文件与系统图标。
9. 不做聊天、任务签到、金币经济、联网推理、账户、云服务或支付。

## 实施原则

建议先用 C#、.NET 10 正式稳定工具链、Win32 小尺寸分层窗口。可使用 WinForms 的消息循环、NativeWindow、托盘等基础设施；不要先搭复杂 UI 控件树。平台与绘制实现隔离，后续可以替换绘制后端。

家园和外出的鼠鼠分别使用必要大小的绘制表面；不得通过一张全屏透明表面实现默认状态。可见区域不变且画面静止时不重绘；隐藏后停止帧循环。不忙等待，不强行拉高系统定时器精度。

核心状态不依赖 UI；使用可替换时钟与固定随机种子测试。位置、显示模式、角色归属、动作进度分别建模。不要为每个窗口创建自己的世界循环。

本轮使用原创占位素材，不下载或复制马里奥等商业游戏素材。不要将占位美术描述为最终品质。

## 交付诚实性

构建、测试、发布命令必须实际执行后才可报告成功。不能执行时明确说明原因，并提供可复现命令。不得编造截图、录屏、CPU 或内存数据。每项桌面验收分别标记通过、失败或未测试。

保留权限边界。GUI 沙箱不等于真实用户桌面；用户可自行运行发布版做交互测试，不要建议关闭沙箱作为默认解决方案。
